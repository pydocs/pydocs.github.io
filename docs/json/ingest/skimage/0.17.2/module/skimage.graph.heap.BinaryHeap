Ùª­jAttributesÙ¯‚ƒÙ°ƒecountcintÙ¹‚Ù§x The number of values in the heap€Ù°ƒflevelscintÙ¹‚Ù§x¯The number of levels in the binary heap (see Notes below). The values are stored in the last level, so 2**levels is the capacity of the heap before another resize is required.€Ù°ƒjmin_levelscintÙ¹‚ƒÙ§x9The minimum number of levels in the heap (relates to the Ù¢„pinitial_capacityÙ „ööelocalpinitial_capacityelocalõÙ§l parameter.)€öpExtended SummaryÙ¯‚‚Ù¹‚Ù§yA binary heap is an object to store values in, optimized in such a way that the minimum (or maximum, but a minimum in this implementation) value can be found in O(log2(N)) time. In this implementation, a reference value (a single integer) can also be stored with each value.€Ù¹‚Ù§xuUse the methods push() and pop() to put in or extract values. In C, use the corresponding push_fast() and pop_fast().€ögMethodsÙ¯‚€öeNotesÙ¯‚†Ù¹‚Ù§y»This implementation stores the binary heap in an array twice as long as the number of elements in the heap. The array is structured in levels, starting at level 0 with a single value, doubling the amount of values in each level. The final level contains the actual values, the level before it contains the pairwise minimum values. The level before that contains the pairwise minimum values of that level, etc. Take a look at this illustration:€Ù¹‚Ù§x€level: 0 11 2222 33333333 4444444444444444 index: 0 12 3456 78901234 5678901234567890                     1          2         3€Ù·xIThe actual values are stored in level 4. The minimum value of position 15Ù¹‚Ù§x¥and 16 is stored in position 7. min(17,18)->8, min(7,8)->3, min(3,4)->1. When adding a value, only the path to the top has to be updated, which takesO(log2(N)) time.€Ù·x<The advantage of this implementation relative to more commonÙ¹‚Ù§yimplementations that swap values when pushing to the array is that data only needs to be swapped once when an element is removed. This means that keeping an array of references along with the values is very inexpensive. Th disadvantage is that if you pop the minimum value, the tree has to be traced from top to bottom and back. So if you only want values and no references, this implementation will probably be slower. If you need references (and maybe cross references to be kept up to date) this implementation will be faster.€öpOther ParametersÙ¯‚€öjParametersÙ¯‚Ù°ƒpinitial_capacitycintÙ¹‚ƒÙ§x“Estimate of the size of the heap, if known in advance. (In any case, the heap will dynamically grow and shrink as required, though never below the Ù¢„pinitial_capacityÙ „ööelocalpinitial_capacityelocalõÙ§b.)€öfRaisesÙ¯‚€öhReceivesÙ¯‚€ögReturnsÙ¯‚€ögSummaryÙ¯‚Ù¹‚Ù§xCA binary heap class that can store values and an integer reference.€öhWarningsÙ¯‚€öeWarnsÙ¯‚€öfYieldsÙ¯‚€ö†iSignaturegSummarypExtended SummaryjParametersjAttributeseNotesx(/skimage/graph/heap.cpython-39-darwin.soön<class 'type'>xskimage.graph.heap.BinaryHeapÙ¯‚€ö€f0.17.2Ù«x BinaryHeap(initial_capacity=128)öxskimage.graph.heap.BinaryHeap€